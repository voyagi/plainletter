"""The evaluation set: letters with expected properties, and the scoring that reads them.

This package is deliberately outside `src/`. It is a measuring instrument for the people changing
the prompts, not part of the product, so it stays out of the wheel and out of the deployed runtime.
"""
